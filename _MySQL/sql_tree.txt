gallery
	pictures:
		picID, location, name, description, price;
	users:
		personID, fname,lname, email;
	basket:
		orderID, personID, picID, amount;
	tags:
		picID, tag;

SELECT pictures.name, pictures.location, pictures.description, pictures.price
FROM pictures, tags
WHERE tags.tag = "art" AND pictures.picID = tags.picID;